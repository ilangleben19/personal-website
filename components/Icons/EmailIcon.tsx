export default function EmailIcon() {
    return (
        <img src="/email-icon.png" style={{
            filter: 'brightness(0) invert(1)',
            maxWidth: '100%',
            maxHeight: '100%',
        }} />
    );
}